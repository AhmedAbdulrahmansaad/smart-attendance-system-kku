# ⚡ دليل النشر السريع - 5 دقائق فقط

## للذين لديهم خبرة ويريدون النشر بسرعة ⚡

---

## 📋 المتطلبات

تأكد من تثبيت:
```bash
node --version  # v18+
git --version
npm install -g supabase
```

---

## 🚀 النشر في 5 خطوات

### 1️⃣ إعداد Supabase (دقيقتين)

```bash
# تسجيل الدخول
supabase login

# ربط المشروع (استبدل PROJECT_ID)
supabase link --project-ref YOUR_PROJECT_ID

# تنفيذ Schema في Supabase SQL Editor
# انسخ محتوى /database_schema.sql والصقه في SQL Editor
```

---

### 2️⃣ نشر Edge Functions (30 ثانية)

```bash
supabase functions deploy server
```

✅ اختبار:
```bash
curl https://YOUR_PROJECT_ID.supabase.co/functions/v1/make-server-90ad488b/health
# يجب أن يرجع: {"status":"ok"}
```

---

### 3️⃣ رفع على GitHub (دقيقة)

```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/YOUR_USERNAME/kku-attendance-system.git
git branch -M main
git push -u origin main
```

---

### 4️⃣ نشر على Vercel (دقيقتين)

1. افتح [vercel.com](https://vercel.com)
2. **Import Project** → اختر repository
3. أضف **Environment Variables**:
   ```
   VITE_SUPABASE_URL=https://YOUR_PROJECT_ID.supabase.co
   VITE_SUPABASE_ANON_KEY=your_anon_key
   ```
4. **Deploy**

---

### 5️⃣ إضافة بيانات تجريبية (30 ثانية)

في Supabase SQL Editor:
```sql
-- إضافة مدرس
INSERT INTO profiles (id, email, full_name, role) 
VALUES (gen_random_uuid(), 'teacher@kku.edu.sa', 'د. محمد أحمد', 'instructor');

-- إضافة طلاب
INSERT INTO profiles (id, email, full_name, role, university_id) 
VALUES 
  (gen_random_uuid(), 'student1@kku.edu.sa', 'أحمد محمد', 'student', '441234567'),
  (gen_random_uuid(), 'student2@kku.edu.sa', 'خالد علي', 'student', '441234568');

-- إضافة مقرر
INSERT INTO courses (course_name, course_code, instructor_id)
SELECT 'برمجة الويب', 'CS301', id FROM profiles WHERE role = 'instructor' LIMIT 1;
```

---

## ✅ التحقق

افتح موقعك على Vercel واضغط F12:
```
✅ Public stats retrieved from REAL DATABASE:
   studentsCount: 2
   instructorsCount: 1
   coursesCount: 1
```

---

## 🔄 التحديثات المستقبلية

### تحديث الكود:
```bash
git add .
git commit -m "Update"
git push
# Vercel ستنشر تلقائياً
```

### تحديث Edge Functions:
```bash
supabase functions deploy server
```

---

## 🆘 حل سريع للمشاكل

| المشكلة | الحل |
|---------|------|
| الأرقام = 0 | أضف بيانات في SQL Editor |
| CORS Error | تحقق من نشر Edge Functions |
| 404 Error | تحقق من Environment Variables |
| Build Failed | تحقق من `node_modules` و `package.json` |

---

## 📞 الدعم السريع

```bash
# Logs الخاصة بـ Edge Functions
supabase functions logs server --follow

# Logs الخاصة بـ Vercel
# في Vercel Dashboard → Deployments → View Function Logs
```

---

## 🎯 الروابط المهمة

```bash
# الحصول على Project ID
supabase projects list

# الحصول على API Keys
# Supabase → Settings → API

# مشاهدة الـ Functions
supabase functions list
```

---

**✅ تم! نظامك الآن live على الإنترنت! 🎉**

---

## 📚 للمزيد من التفاصيل

- 📖 `/📚_دليل_النشر_الشامل_خطوة_بخطوة.md` - دليل مفصل للمبتدئين
- 🎓 `/🎓_دليل_المبتدئين_للنشر.md` - شرح من الصفر
- 🚀 `/DEPLOYMENT_GUIDE.md` - دليل تقني شامل
