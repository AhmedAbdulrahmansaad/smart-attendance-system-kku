# 🎓 دليل المبتدئين للنشر - من الصفر إلى النهاية

## للذين ليس لديهم خبرة سابقة ✨

---

# 📌 ما الذي سنفعله؟

سننشر نظام الحضور الذكي على الإنترنت بحيث يكون:
1. ✅ **مجاني 100%**
2. ✅ **متاح على رابط حقيقي** (مثل: `https://your-project.vercel.app`)
3. ✅ **مربوط بقاعدة بيانات حقيقية**
4. ✅ **يعمل بشكل كامل**

---

# 🛠️ التحضيرات (افعلها مرة واحدة فقط)

## 1️⃣ تثبيت Node.js

### ما هو Node.js؟
بيئة تشغيل لتطبيقات JavaScript على جهازك.

### كيف أثبته؟

#### على Windows:
1. افتح [nodejs.org](https://nodejs.org)
2. اضغط على **Download** (الإصدار LTS)
3. شغّل الملف المحمّل
4. اضغط **Next** → **Next** → **Install**
5. انتظر حتى ينتهي التثبيت

#### التحقق من التثبيت:
افتح **Command Prompt** (اكتب `cmd` في قائمة ابدأ):
```bash
node --version
```

يجب أن ترى رقم الإصدار (مثل: `v20.10.0`)

---

## 2️⃣ تثبيت Git

### ما هو Git؟
أداة للتحكم في الإصدارات ورفع الكود على GitHub.

### كيف أثبته؟

#### على Windows:
1. افتح [git-scm.com](https://git-scm.com)
2. اضغط **Download for Windows**
3. شغّل الملف المحمّل
4. اضغط **Next** على كل شيء (الإعدادات الافتراضية جيدة)

#### التحقق من التثبيت:
```bash
git --version
```

يجب أن ترى رقم الإصدار (مثل: `git version 2.43.0`)

---

## 3️⃣ تثبيت Supabase CLI

### ما هو Supabase CLI؟
أداة لنشر Edge Functions على Supabase.

### كيف أثبته؟

افتح **Command Prompt** واكتب:
```bash
npm install -g supabase
```

انتظر حتى ينتهي التثبيت (قد يستغرق 1-2 دقيقة).

#### التحقق من التثبيت:
```bash
supabase --version
```

يجب أن ترى رقم الإصدار (مثل: `1.142.2`)

---

## 4️⃣ إنشاء الحسابات المطلوبة (مجانية!)

### حساب Supabase:
1. افتح [supabase.com](https://supabase.com)
2. اضغط **Start Your Project**
3. سجّل باستخدام GitHub أو Email

### حساب Vercel:
1. افتح [vercel.com](https://vercel.com)
2. اضغط **Sign Up**
3. **مهم:** اختر **Sign up with GitHub** (سيسهل الربط لاحقاً)

### حساب GitHub (إذا لم يكن لديك):
1. افتح [github.com](https://github.com)
2. اضغط **Sign Up**
3. أكمل التسجيل

---

# 🚀 الخطوة 1: إنشاء مشروع على Supabase

## 1. افتح Supabase Dashboard
- اذهب إلى [app.supabase.com](https://app.supabase.com)

## 2. إنشاء مشروع جديد
1. اضغط **New Project**
2. اختر **Organization** (أو أنشئ واحدة جديدة)
3. املأ البيانات:
   - **Name**: `KKU Attendance System`
   - **Database Password**: اختر كلمة مرور قوية (احفظها!)
   - **Region**: `West Asia (Mumbai)` (أقرب منطقة للسعودية)
   - **Pricing Plan**: `Free` (مجاني)
4. اضغط **Create new project**
5. انتظر 2-3 دقائق حتى يجهز المشروع

---

# 🗄️ الخطوة 2: إعداد قاعدة البيانات

## 1. فتح SQL Editor
في Dashboard الخاص بـ Supabase:
1. في القائمة الجانبية اليسرى، اضغط على **SQL Editor**
2. ستظهر صفحة فارغة لكتابة SQL

## 2. نسخ ولصق Schema

### في Figma Make (هذا المشروع):
1. افتح ملف `/database_schema.sql`
2. اضغط **Ctrl+A** لتحديد الكل
3. اضغط **Ctrl+C** للنسخ

### في Supabase SQL Editor:
1. اضغط **Ctrl+V** للصق
2. اضغط **Run** (أو اضغط **Ctrl+Enter**)
3. انتظر حتى يكتمل التنفيذ (10-20 ثانية)

### ✅ رسالة النجاح:
ستظهر رسالة باللون الأخضر:
```
Success. No rows returned
```

## 3. التحقق من إنشاء الجداول

شغّل هذا الاستعلام في SQL Editor:
```sql
SELECT tablename 
FROM pg_tables 
WHERE schemaname = 'public'
ORDER BY tablename;
```

يجب أن ترى:
```
attendance
courses
enrollments
profiles
schedules
sessions
```

✅ **رائع! قاعدة البيانات جاهزة!**

---

# 🔑 الخطوة 3: الحصول على مفاتيح Supabase

## 1. فتح صفحة API Settings
في Dashboard:
1. اضغط على **Settings** (رمز الترس) في القائمة الجانبية
2. اضغط على **API**

## 2. نسخ المفاتيح المطلوبة

ستجد ثلاثة أشياء مهمة:

### A) Project URL
```
https://abcdefghijklmnop.supabase.co
```
**انسخه!** سنحتاجه لاحقاً.

### B) anon public key
```
eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImFiY2RlZmdoaWprbG1ub3AiLCJyb2xlIjoiYW5vbiIsImlhdCI6MTYxMjQ1NjAwMCwiZXhwIjoxOTI4MDMyMDAwfQ...
```
**انسخه!** سنحتاجه لاحقاً.

### C) service_role secret key
⚠️ **مهم جداً - لا تشاركه مع أحد!**
```
eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImFiY2RlZmdoaWprbG1ub3AiLCJyb2xlIjoic2VydmljZV9yb2xlIiwiaWF0IjoxNjEyNDU2MDAwLCJleHAiOjE5MjgwMzIwMDB9...
```
**انسخه!** سنحتاجه لاحقاً.

## 3. حفظ المفاتيح
افتح **Notepad** (المفكرة) والصق المفاتيح الثلاثة:
```
Project URL: https://abcdefghijklmnop.supabase.co
Project ID: abcdefghijklmnop
Anon Key: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
Service Role Key: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

احفظ الملف باسم `supabase-keys.txt` (ولا ترفعه على GitHub!)

---

# 📤 الخطوة 4: نشر Edge Functions على Supabase

## 1. فتح Terminal في مجلد المشروع

### على Windows:
1. افتح مجلد المشروع في File Explorer
2. اضغط على شريط العنوان (Address Bar) في الأعلى
3. اكتب `cmd` واضغط **Enter**
4. ستفتح نافذة Command Prompt في مجلد المشروع

### على Mac:
1. افتح Terminal
2. اكتب `cd ` (بمسافة)
3. اسحب مجلد المشروع إلى Terminal
4. اضغط Enter

## 2. تسجيل الدخول إلى Supabase CLI

```bash
supabase login
```

- ستفتح نافذة متصفح
- سجل الدخول بحساب Supabase الخاص بك
- ارجع إلى Terminal

ستظهر رسالة:
```
✔ Logged in successfully!
```

## 3. ربط المشروع

استخدم الـ **Project ID** الذي نسخته سابقاً:

```bash
supabase link --project-ref abcdefghijklmnop
```

⚠️ **استبدل `abcdefghijklmnop` بالـ ID الخاص بك!**

ستظهر رسالة:
```
✔ Linked to project abcdefghijklmnop
```

## 4. نشر Edge Functions 🚀

```bash
supabase functions deploy server
```

انتظر 30-60 ثانية...

### ✅ رسالة النجاح:
```
Bundling server...
Deploying server (project ref: abcdefghijklmnop)
Deployed function server version: v1
✔ Deployed Function server
```

🎉 **تم النشر بنجاح!**

## 5. اختبار Edge Function

### الاختبار من Terminal:
```bash
curl https://abcdefghijklmnop.supabase.co/functions/v1/make-server-90ad488b/health
```

يجب أن ترى:
```json
{"status":"ok"}
```

### الاختبار من المتصفح:
افتح هذا الرابط:
```
https://abcdefghijklmnop.supabase.co/functions/v1/make-server-90ad488b/stats/public
```

يجب أن ترى:
```json
{
  "success": true,
  "stats": {
    "studentsCount": 0,
    "instructorsCount": 0,
    "coursesCount": 0,
    "attendanceRate": "99.8"
  },
  "source": "real_database"
}
```

✅ **رائع! Edge Functions تعمل!**

---

# 🌐 الخطوة 5: رفع المشروع على GitHub

## 1. تهيئة Git في مجلد المشروع

في Terminal (نفس النافذة من قبل):

```bash
git init
```

## 2. إضافة جميع الملفات

```bash
git add .
```

## 3. عمل Commit

```bash
git commit -m "Initial commit - KKU Attendance System"
```

## 4. إنشاء Repository على GitHub

1. افتح [github.com](https://github.com)
2. اضغط على **+** في الزاوية اليمنى العليا
3. اضغط **New repository**
4. املأ البيانات:
   - **Repository name**: `kku-attendance-system`
   - **Description**: `نظام الحضور الذكي - جامعة الملك خالد`
   - **Private**: ✅ (اختر Private للخصوصية)
   - **لا تضف** README أو .gitignore أو License
5. اضغط **Create repository**

## 5. ربط المشروع المحلي بـ GitHub

بعد إنشاء Repository، ستظهر تعليمات. انسخ الأمر الذي يبدأ بـ:
```bash
git remote add origin https://github.com/YOUR_USERNAME/kku-attendance-system.git
```

⚠️ **استبدل `YOUR_USERNAME` باسم المستخدم الخاص بك على GitHub!**

ثم:
```bash
git branch -M main
git push -u origin main
```

قد يطلب منك GitHub اسم المستخدم وكلمة المرور.

### ✅ رسالة النجاح:
```
Enumerating objects: 150, done.
Counting objects: 100% (150/150), done.
Writing objects: 100% (150/150), 1.50 MiB | 1.00 MiB/s, done.
Total 150 (delta 0), reused 0 (delta 0)
To https://github.com/YOUR_USERNAME/kku-attendance-system.git
 * [new branch]      main -> main
```

---

# 🚀 الخطوة 6: نشر المشروع على Vercel

## 1. فتح Vercel Dashboard

افتح [vercel.com](https://vercel.com) وسجل الدخول.

## 2. إنشاء مشروع جديد

1. اضغط **Add New...** → **Project**
2. ستظهر قائمة بـ Repositories من GitHub
3. ابحث عن `kku-attendance-system`
4. اضغط **Import**

## 3. إعداد المشروع

### Framework Preset:
اختر **Vite** (يجب أن يكتشفها تلقائياً)

### Root Directory:
اتركها كما هي `/`

### Build Command:
```bash
npm run build
```

### Output Directory:
```
dist
```

## 4. إضافة Environment Variables (مهم جداً!)

⚠️ **لا تضغط Deploy بعد!** يجب إضافة المتغيرات أولاً!

### اضغط على **Environment Variables**

أضف المتغيرات التالية واحداً تلو الآخر:

#### المتغير 1:
- **Name**: `VITE_SUPABASE_URL`
- **Value**: `https://abcdefghijklmnop.supabase.co` (استبدل بـ URL الخاص بك)
- اضغط **Add**

#### المتغير 2:
- **Name**: `VITE_SUPABASE_ANON_KEY`
- **Value**: الصق **anon public key** من ملف `supabase-keys.txt`
- اضغط **Add**

⚠️ **ملاحظة:** لا نحتاج إضافة `service_role_key` هنا لأنه يُستخدم فقط في Edge Functions (وليس Frontend).

## 5. النشر! 🚀

اضغط **Deploy**

انتظر 2-4 دقائق حتى يكتمل النشر...

### ✅ رسالة النجاح:
```
🎉 Congratulations! Your project has been deployed!
```

اضغط **Visit** أو **Go to Dashboard**

---

# 🎯 الخطوة 7: التحقق من عمل النظام

## 1. فتح الموقع

انسخ رابط موقعك (مثل: `https://kku-attendance-system.vercel.app`)

## 2. فتح Console

اضغط **F12** لفتح Developer Tools، ثم اضغط **Console**

## 3. إعادة تحميل الصفحة

اضغط **Ctrl+R** لإعادة تحميل الصفحة

## 4. البحث عن الرسائل

يجب أن ترى في Console:
```
🔍 Fetching landing stats from API...
📍 URL: https://abcdefghijklmnop.supabase.co/functions/v1/make-server-90ad488b/stats/public
📡 Response status: 200
✅ Landing page stats from database: 
   studentsCount: 0
   instructorsCount: 0
   coursesCount: 0
   attendanceRate: "99.8"
```

### 📊 الأرقام في الصفحة الرئيسية:
يجب أن ترى:
- 👥 **0** طالب نشط
- 👨‍🏫 **0** عضو هيئة تدريس
- 📚 **0** مقرر دراسي
- 🎯 **99.8%** دقة النظام

⚠️ **لماذا 0؟** لأن قاعدة البيانات فارغة! سنضيف بيانات الآن.

---

# 👥 الخطوة 8: إضافة بيانات تجريبية

## 1. فتح Supabase SQL Editor

ارجع إلى [app.supabase.com](https://app.supabase.com) → SQL Editor

## 2. إضافة مستخدمين

الصق هذا الكود:

```sql
-- إضافة مدرس
INSERT INTO profiles (id, email, full_name, role, university_id)
VALUES (
  gen_random_uuid(),
  'instructor1@kku.edu.sa',
  'د. محمد أحمد السعيد',
  'instructor',
  NULL
);

-- إضافة 3 طلاب
INSERT INTO profiles (id, email, full_name, role, university_id)
VALUES 
  (gen_random_uuid(), 'student1@kku.edu.sa', 'أحمد محمد علي', 'student', '441234567'),
  (gen_random_uuid(), 'student2@kku.edu.sa', 'خالد عبدالله إبراهيم', 'student', '441234568'),
  (gen_random_uuid(), 'student3@kku.edu.sa', 'عبدالرحمن سعد محمد', 'student', '441234569');
```

اضغط **Run**

### ✅ النتيجة:
```
Success. 4 rows affected.
```

## 3. إضافة مقررات

```sql
-- إضافة مقرر
INSERT INTO courses (id, course_name, course_code, instructor_id)
SELECT 
  gen_random_uuid(),
  'برمجة الويب المتقدمة',
  'CS301',
  id
FROM profiles 
WHERE role = 'instructor' 
LIMIT 1;

-- إضافة مقرر آخر
INSERT INTO courses (id, course_name, course_code, instructor_id)
SELECT 
  gen_random_uuid(),
  'قواعد البيانات المتقدمة',
  'CS302',
  id
FROM profiles 
WHERE role = 'instructor' 
LIMIT 1;
```

اضغط **Run**

---

# 🎉 الخطوة 9: مشاهدة النتيجة النهائية!

## 1. ارجع إلى موقعك على Vercel

## 2. أعد تحميل الصفحة (Ctrl+R)

## 3. شاهد الأرقام الحقيقية!

يجب أن ترى الآن:
- 👥 **3** طلاب نشطين
- 👨‍🏫 **1** عضو هيئة تدريس
- 📚 **2** مقرر دراسي
- 🎯 **99.8%** دقة النظام

## 4. في Console:

```
✅ Public stats retrieved from REAL DATABASE:
   studentsCount: 3
   instructorsCount: 1
   coursesCount: 2
   attendanceRate: "99.8"
```

---

# 🎊 تهانينا! 🎊

## لقد نجحت في:

✅ **إنشاء مشروع Supabase** مع قاعدة بيانات SQL حقيقية
✅ **نشر Edge Functions** على Supabase
✅ **رفع المشروع على GitHub**
✅ **نشر الموقع على Vercel**
✅ **ربط كل شيء معاً**
✅ **إضافة بيانات حقيقية**
✅ **مشاهدة الأرقام الحقيقية على الموقع**

---

# 📋 الروابط المهمة (احفظها!)

```
🌐 موقعك على Vercel:
https://your-project.vercel.app

🗄️ Supabase Dashboard:
https://app.supabase.com

💻 GitHub Repository:
https://github.com/YOUR_USERNAME/kku-attendance-system

🔧 Edge Functions URL:
https://YOUR_PROJECT_ID.supabase.co/functions/v1/make-server-90ad488b
```

---

# 🔄 كيف أحدث المشروع في المستقبل؟

## إذا غيرت شيئاً في الكود:

```bash
# 1. في Terminal
git add .
git commit -m "وصف التغيير"
git push

# 2. Vercel ستقوم بـ re-deploy تلقائياً!
# لا تحتاج فعل أي شيء آخر
```

## إذا غيرت شيئاً في Edge Functions:

```bash
supabase functions deploy server
```

---

# ❓ أسئلة شائعة

## س1: هل النشر مجاني؟
**ج:** نعم! كل من Supabase و Vercel يقدمون خطة مجانية كافية لمشروع تخرج.

## س2: كم يستغرق النشر؟
**ج:** حوالي 30-45 دقيقة للمرة الأولى.

## س3: هل يمكنني تغيير الرابط؟
**ج:** نعم! في Vercel → Settings → Domains، يمكنك إضافة domain مخصص.

## س4: ماذا لو نسيت كلمة مرور Supabase Database؟
**ج:** يمكنك إعادة تعيينها من Supabase Dashboard → Settings → Database.

## س5: هل البيانات آمنة؟
**ج:** نعم! Supabase يستخدم تشفير SSL/TLS، و RLS Policies مفعّلة.

---

**🎉 مبروك مرة أخرى! نظامك الآن على الإنترنت! 🎉**
