# 📋 مواصفات مشروع نظام الحضور الذكي - جامعة الملك خالد
## King Khalid University Smart Attendance System - Complete Requirements

---

## 📌 نظرة عامة على المشروع

أحتاج تطوير **نظام حضور ذكي متكامل** لجامعة الملك خالد من الصفر بشكل احترافي وكامل، يعمل بشكل فعلي حقيقي 100% مع واجهة مستخدم احترافية ودعم كامل للغتين العربية والإنجليزية.

---

## 🎯 الأهداف الرئيسية

1. **نظام حضور إلكتروني كامل** للطلاب والمدرسين
2. **إدارة مقررات دراسية** متكاملة
3. **جلسات بث مباشر** بالصوت والفيديو (Jitsi Meet)
4. **تسجيل حضور تلقائي** عند الانضمام للجلسات
5. **نظام إشعارات فورية** Real-time
6. **أمان متقدم** ضد التلاعب
7. **واجهة احترافية** بألوان جامعة الملك خالد
8. **دعم كامل RTL/LTR** للغتين

---

## 👥 الأدوار الأربعة (User Roles)

### 1. المدير (Admin) 👨‍💼
**الصلاحيات:**
- إضافة وإدارة جميع المستخدمين (طلاب، مدرسين، مشرفين)
- إنشاء وحذف وتعديل المقررات الدراسية
- تعيين المدرسين للمقررات
- تسجيل الطلاب في المقررات
- عرض جميع الإحصائيات والتقارير
- إدارة النظام بالكامل
- إرسال إشعارات لجميع المستخدمين

**لوحة التحكم:**
- إحصائيات شاملة (عدد الطلاب، المدرسين، المقررات، معدل الحضور)
- جداول إدارية (Users, Courses, Sessions, Attendance)
- رسوم بيانية Charts للحضور والغياب
- تقارير مفصلة

---

### 2. المدرس (Instructor) 👨‍🏫
**الصلاحيات:**
- عرض المقررات المُسندة له فقط
- إنشاء جلسات حضور (Attendance Sessions) بكود عشوائي
- إنشاء جلسات بث مباشر (Live Sessions) بـ Jitsi Meet
- بدء وإنهاء الجلسات
- عرض سجلات حضور الطلاب في مقرراته
- تصدير تقارير الحضور Excel/PDF
- إرسال إشعارات للطلاب المسجلين

**لوحة التحكم:**
- مقرراته الدراسية فقط
- جلساته النشطة والسابقة
- عدد الطلاب المسجلين في كل مقرر
- معدل الحضور لكل مقرر
- إحصائيات تفصيلية لكل مقرر

---

### 3. الطالب (Student) 👨‍🎓
**الصلاحيات:**
- عرض المقررات المسجل فيها فقط
- تسجيل الحضور بإدخال كود الجلسة
- الانضمام لجلسات البث المباشر
- عرض سجل حضوره الشخصي
- عرض معدل حضوره في كل مقرر
- استلام الإشعارات (بدء جلسة، تذكير، إلخ)

**لوحة التحكم:**
- مقرراته المسجل فيها
- سجل حضوره الكامل
- معدل الحضور لكل مقرر
- الجلسات القادمة والحالية
- إشعارات فورية

---

### 4. المشرف (Supervisor) 👨‍💼
**الصلاحيات:**
- عرض جميع المقررات والجلسات (قراءة فقط)
- عرض تقارير الحضور والغياب
- مراقبة النظام بشكل عام
- عرض الإحصائيات الشاملة
- تصدير التقارير

**لوحة التحكم:**
- إحصائيات عامة
- تقارير شاملة
- مراقبة الأداء

---

## 🔐 نظام المصادقة والتسجيل

### شاشة تسجيل الدخول / التسجيل:

**متطلبات التسجيل:**

1. **البريد الإلكتروني (إلزامي للجميع):**
   - يجب أن يكون من نطاق جامعة الملك خالد: `@kku.edu.sa`
   - مثال: `ahmed.mohammed@kku.edu.sa`
   - التحقق: Validation على الـ Frontend والـ Backend

2. **كلمة المرور (إلزامية للجميع):**
   - الحد الأدنى: 8 أحرف
   - يجب أن تحتوي على: حرف كبير، حرف صغير، رقم
   - تشفير: bcrypt أو Supabase Auth

3. **الاسم الكامل (إلزامي للجميع):**
   - بالعربية أو الإنجليزية
   - مثال: `أحمد محمد علي` أو `Ahmed Mohammed Ali`

4. **الدور (Role - إلزامي للجميع):**
   - خيارات: Admin, Instructor, Student, Supervisor
   - Dropdown/Select

5. **الرقم الجامعي (إلزامي للطلاب فقط):**
   - 9 أرقام تبدأ بـ `44`
   - مثال: `441234567`
   - التحقق: يجب أن يكون فريداً في النظام
   - Regex: `^44\d{7}$`

**التصميم:**
- شاشة واحدة بـ Tabs (تسجيل دخول / تسجيل جديد)
- ألوان جامعة الملك خالد: الأخضر الداكن `#006747`
- شعار الجامعة في الأعلى
- دعم RTL/LTR تلقائي حسب اللغة
- رسائل خطأ واضحة بالعربية والإنجليزية

**الأمان:**
- JWT Token Authentication
- Session Management
- Prevent Concurrent Login (منع تسجيل دخول من جهازين)
- Device Fingerprint Tracking

---

## 📚 إدارة المقررات الدراسية (Courses)

### البيانات المطلوبة لكل مقرر:

1. **اسم المادة (إلزامي)** - `course_name`
   - مثال: "برمجة تطبيقات الويب"
   - دعم العربية والإنجليزية

2. **رمز المادة (إلزامي)** - `course_code`
   - مثال: "CS301"
   - يجب أن يكون فريداً

3. **المدرس المسؤول (إلزامي)** - `instructor_id`
   - اختيار من قائمة المدرسين المسجلين
   - Dropdown/Select

4. **القسم (اختياري)** - `department`
   - مثال: "علوم الحاسب"

5. **عدد الساعات (اختياري)** - `credits`
   - مثال: 3 ساعات

6. **الفصل الدراسي (اختياري)** - `semester`
   - مثال: "الفصل الأول"

7. **السنة الدراسية (اختياري)** - `year`
   - مثال: "1446"

8. **الوصف (اختياري)** - `description`
   - نص حر

9. **الحالة** - `is_active`
   - نشط / غير نشط

**الميزات:**
- إضافة مقرر جديد (Admin, Instructor)
- تعديل مقرر (Admin)
- حذف مقرر (Admin)
- عرض قائمة المقررات بفلترة وبحث
- تصدير قائمة المقررات Excel

---

## 👨‍🎓 تسجيل الطلاب في المقررات (Enrollments)

### العملية:

1. **المدير أو المدرس** يختار:
   - الطالب (من قائمة الطلاب)
   - المقرر (من قائمة المقررات)
   - تاريخ التسجيل (تلقائي)

2. **التحقق:**
   - التأكد من عدم تسجيل الطالب مسبقاً
   - التأكد من أن المقرر نشط

3. **الميزات:**
   - إضافة تسجيل جديد
   - إلغاء تسجيل
   - عرض الطلاب المسجلين في مقرر
   - عرض المقررات المسجل فيها طالب
   - تسجيل دفعة من الطلاب (Bulk Enrollment)
   - تصدير قائمة التسجيلات Excel

---

## 📅 جلسات الحضور (Attendance Sessions)

### نوعان من الجلسات:

#### 1. جلسة حضور عادية (Attendance Session):

**البيانات المطلوبة:**
- المقرر (إلزامي)
- تاريخ الجلسة (إلزامي)
- وقت البدء (إلزامي)
- المدة بالدقائق (إلزامي) - مثال: 15 دقيقة
- نوع الجلسة (محاضرة، معمل، تمارين)
- الموقع (اختياري) - مثال: "مبنى A - قاعة 101"

**الآلية:**
1. المدرس ينشئ الجلسة
2. النظام يُولّد **كود عشوائي** (6-8 أحرف) - مثال: `AB12CD`
3. المدرس يعرض الكود للطلاب (شاشة عرض أو يكتبه على السبورة)
4. الطالب يدخل الكود في تطبيقه
5. النظام يسجل الحضور تلقائياً

**الميزات:**
- توليد كود عشوائي جديد لكل جلسة
- عرض الكود بخط كبير وواضح
- تحديث العداد التنازلي (Countdown) لانتهاء الجلسة
- عرض عدد الطلاب الحاضرين Real-time
- إمكانية إنهاء الجلسة يدوياً
- تفعيل/إيقاف الجلسة

---

#### 2. جلسة بث مباشر (Live Session):

**البيانات المطلوبة:**
- المقرر (إلزامي)
- عنوان الجلسة (إلزامي)
- وصف الجلسة (اختياري)
- وقت البدء المجدول (إلزامي)
- وقت الانتهاء المجدول (إلزامي)

**الآلية:**
1. المدرس ينشئ جلسة بث مباشر
2. النظام يُنشئ **غرفة Jitsi Meet** تلقائياً
3. يُولّد رابط فريد: `https://meet.jit.si/kku_[RANDOM_CODE]`
4. النظام يُرسل **إشعار فوري** لجميع الطلاب المسجلين
5. المدرس يضغط "بدء الجلسة" → الحالة تصبح "Live"
6. الطلاب ينضمون للجلسة عبر الرابط
7. النظام يسجل حضور الطالب **تلقائياً عند الانضمام**

**الميزات:**
- تكامل كامل مع Jitsi Meet
- دعم الصوت والفيديو والمشاركة الشاشة
- تسجيل حضور تلقائي عند الانضمام
- إشعارات فورية للطلاب
- عداد للطلاب المتصلين Real-time
- حالات الجلسة: Scheduled, Live, Ended, Cancelled
- إمكانية إعادة استخدام الرابط لجلسات متعددة (اختياري)

**التقنيات المطلوبة:**
- **Jitsi Meet API** أو iframe embed
- لا تستخدم مكتبات أخرى (Zoom, Google Meet)
- يجب أن يكون Jitsi مجاني ومفتوح المصدر

---

## ✅ تسجيل الحضور (Attendance)

### الطرق المدعومة:

#### 1. تسجيل بالكود (Code-based):
- الطالب يدخل الكود في حقل نصي
- النظام يتحقق من:
  - صحة الكود
  - صلاحية الجلسة (لم تنته)
  - تسجيل الطالب في المقرر
  - عدم تسجيل الحضور مسبقاً
- يسجل: Student ID, Session ID, Timestamp, Status (Present)

#### 2. تسجيل تلقائي (Live Session):
- عند انضمام الطالب لجلسة Jitsi
- النظام يكتشف انضمامه تلقائياً (عبر Jitsi API)
- يسجل الحضور دون تدخل الطالب

### البيانات المسجلة:
- معرّف الطالب
- معرّف الجلسة
- معرّف المقرر
- الحالة: حاضر (Present) / غائب (Absent) / متأخر (Late)
- وقت التسجيل الدقيق
- Device Fingerprint (بصمة الجهاز)
- الموقع الجغرافي (اختياري - إذا سمح الطالب)

### الأمان:
- **منع التلاعب:**
  - تسجيل Device Fingerprint لكل طالب
  - منع تسجيل الحضور من جهازين في نفس الوقت
  - تسجيل IP Address
  - تحديد مدة زمنية قصيرة للتسجيل

- **التحقق:**
  - التأكد من تسجيل الطالب في المقرر
  - التأكد من صلاحية الجلسة
  - منع التسجيل المتكرر

---

## 🔔 نظام الإشعارات (Notifications)

### أنواع الإشعارات:

1. **إشعارات فورية (Real-time):**
   - بدء جلسة بث مباشر
   - إنشاء جلسة حضور جديدة
   - إلغاء جلسة
   - تذكير قبل بدء الجلسة (5 دقائق)

2. **إشعارات عامة:**
   - إضافة مقرر جديد
   - تغيير في جدول المقرر
   - إعلانات إدارية

### التقنيات:
- **Supabase Realtime** للإشعارات الفورية
- عرض الإشعارات في Notification Center
- إشارة Badge على الأيقونة (عدد الإشعارات غير المقروءة)
- صوت تنبيه (اختياري)
- إمكانية تعليم الإشعار كمقروء

### البيانات:
- العنوان (عربي/إنجليزي)
- المحتوى
- النوع (info, success, warning, error)
- الحالة (مقروء/غير مقروء)
- الوقت
- الرابط (اختياري - للانتقال المباشر)

---

## 📊 الإحصائيات والتقارير

### 1. إحصائيات عامة (Public Stats - صفحة الهبوط):
- عدد الطلاب المسجلين
- عدد المدرسين
- عدد المقررات
- معدل الحضور العام

### 2. إحصائيات المدير (Admin Dashboard):
- إجمالي المستخدمين (بحسب الدور)
- إجمالي المقررات النشطة
- إجمالي الجلسات (اليوم، هذا الأسبوع، هذا الشهر)
- معدل الحضور العام
- الجلسات النشطة الآن
- رسوم بيانية:
  - خط بياني لمعدل الحضور اليومي/الأسبوعي/الشهري
  - دائرة بيانية لتوزيع الطلاب حسب المقررات
  - عمود بياني لمقارنة الحضور بين المقررات

### 3. إحصائيات المدرس (Instructor Dashboard):
- عدد مقرراته
- عدد الطلاب الإجمالي في مقرراته
- عدد الجلسات المنشأة
- معدل الحضور لكل مقرر
- رسوم بيانية لكل مقرر:
  - معدل حضور الطلاب
  - أكثر الطلاب حضوراً/غياباً

### 4. إحصائيات الطالب (Student Dashboard):
- عدد المقررات المسجل فيها
- معدل حضوره العام
- معدل حضوره في كل مقرر
- عدد الجلسات الحاضر فيها / الغائب عنها
- رسم بياني لسجل حضوره

### التصدير:
- تصدير التقارير بصيغة **Excel**
- تصدير التقارير بصيغة **PDF**
- تصدير لقائمة معينة أو مقرر محدد

---

## 🎨 التصميم والواجهة (UI/UX)

### 1. الألوان (Color Scheme):

**الألوان الرسمية لجامعة الملك خالد:**
- **الأخضر الداكن (Primary):** `#006747`
- **الذهبي (Secondary):** `#D4AF37`
- **أبيض:** `#FFFFFF`
- **رمادي فاتح:** `#F5F5F5`
- **رمادي داكن:** `#333333`

**الاستخدام:**
- الـ Header: خلفية خضراء داكنة `#006747`
- الأزرار الرئيسية: خلفية خضراء `#006747`
- الأزرار الثانوية: إطار أخضر، نص أخضر
- الروابط: أخضر `#006747`
- الحالات:
  - نجاح: أخضر `#10B981`
  - خطأ: أحمر `#EF4444`
  - تحذير: برتقالي `#F59E0B`
  - معلومات: أزرق `#3B82F6`

---

### 2. الخطوط (Typography):

**العربية:**
- استخدم: `'Cairo', 'Tajawal', 'Almarai', sans-serif`
- Font weights: 400 (Regular), 500 (Medium), 700 (Bold)

**الإنجليزية:**
- استخدم: `'Inter', 'Roboto', sans-serif`
- Font weights: 400, 500, 600, 700

**الأحجام:**
- العناوين الرئيسية: 24px - 32px
- العناوين الفرعية: 18px - 22px
- النصوص العادية: 14px - 16px
- النصوص الصغيرة: 12px - 13px

---

### 3. دعم اللغتين (Internationalization):

**متطلبات:**
- زر تبديل اللغة في الـ Header
- جميع النصوص مترجمة (لا نص ثابت hardcoded)
- دعم RTL للعربية، LTL للإنجليزية
- تغيير اتجاه الـ Layout تلقائياً
- تغيير الخطوط تلقائياً
- حفظ تفضيل اللغة في Local Storage

**الترجمة:**
- استخدم مكتبة i18n (مثل `react-i18next` أو `next-intl`)
- ملفات ترجمة منفصلة: `ar.json`, `en.json`

---

### 4. الاستجابة (Responsive Design):

**Breakpoints:**
- Mobile: 320px - 767px
- Tablet: 768px - 1023px
- Desktop: 1024px وأكثر

**متطلبات:**
- يجب أن يعمل النظام بشكل ممتاز على الجوال
- القوائم تتحول إلى Hamburger Menu على الجوال
- الجداول scrollable أفقياً على الشاشات الصغيرة
- الأزرار والحقول بأحجام مناسبة للمس

---

### 5. المكونات (Components):

**Navbar:**
- شعار جامعة الملك خالد (يسار/يمين حسب اللغة)
- روابط التنقل
- Notification Bell (مع Badge)
- تبديل اللغة
- معلومات المستخدم + Dropdown (تسجيل خروج، الإعدادات)

**Sidebar (في لوحات التحكم):**
- أيقونات مع نصوص
- تمييز الصفحة الحالية
- قابل للطي على الشاشات الصغيرة

**Cards:**
- بظل خفيف `box-shadow`
- حواف دائرية `border-radius: 8px`
- خلفية بيضاء

**Tables:**
- Header ملون (أخضر فاتح)
- Hover effect على الصفوف
- Pagination
- فلترة وبحث
- أيقونات إجراءات (Edit, Delete, View)

**Forms:**
- Labels واضحة
- حقول بإطار ملون عند Focus
- رسائل خطأ بالأحمر تحت الحقل
- Validation على الـ Frontend والـ Backend

**Buttons:**
- Primary: خلفية خضراء، نص أبيض
- Secondary: إطار أخضر، نص أخضر
- Danger: خلفية حمراء
- أحجام: Small, Medium, Large
- حالات: Normal, Hover, Disabled, Loading

**Modals:**
- خلفية شفافة رمادية
- Modal بالمنتصف
- زر إغلاق (X)
- عنوان ومحتوى وأزرار إجراءات

**Toasts/Alerts:**
- ظهور من أعلى اليمين أو اليسار
- ألوان حسب النوع (Success, Error, Warning, Info)
- إغلاق تلقائي بعد 5 ثوان
- زر إغلاق يدوي

---

## 💾 قاعدة البيانات (Database Schema)

### التقنية المستخدمة:
- **Supabase (PostgreSQL)**
- يجب استخدام **Supabase** فقط (لا Firebase، لا MongoDB)

### الجداول المطلوبة:

#### 1. **profiles** (المستخدمون)
```sql
id (UUID, Primary Key) - من Supabase Auth
email (Text, Unique, Not Null)
full_name (Text, Not Null)
role (Enum: admin, instructor, student, supervisor, Not Null)
university_id (Text, Unique, Nullable) - للطلاب فقط
avatar_url (Text, Nullable)
phone (Text, Nullable)
created_at (Timestamp, Default: now())
updated_at (Timestamp, Default: now())
```

#### 2. **courses** (المقررات)
```sql
id (UUID, Primary Key)
course_name (Text, Not Null)
course_code (Text, Unique, Not Null)
instructor_id (UUID, Foreign Key → profiles.id, Nullable)
department (Text, Nullable)
credits (Integer, Nullable)
semester (Text, Nullable)
year (Text, Nullable)
description (Text, Nullable)
is_active (Boolean, Default: true)
created_at (Timestamp)
updated_at (Timestamp)
```

#### 3. **enrollments** (تسجيل الطلاب)
```sql
id (UUID, Primary Key)
student_id (UUID, Foreign Key → profiles.id, Not Null)
course_id (UUID, Foreign Key → courses.id, Not Null)
enrolled_at (Timestamp, Default: now())
status (Enum: active, dropped, completed, Default: active)

UNIQUE (student_id, course_id)
```

#### 4. **sessions** (جلسات الحضور)
```sql
id (UUID, Primary Key)
course_id (UUID, Foreign Key → courses.id, Not Null)
code (Text, Unique, Not Null) - كود الجلسة
session_date (Date, Not Null)
start_time (Time, Nullable)
end_time (Time, Nullable)
session_type (Enum: lecture, lab, tutorial, Default: lecture)
location (Text, Nullable)
active (Boolean, Default: true)
expires_at (Timestamp, Not Null)
created_at (Timestamp)
updated_at (Timestamp)
```

#### 5. **attendance** (سجل الحضور)
```sql
id (UUID, Primary Key)
student_id (UUID, Foreign Key → profiles.id, Not Null)
session_id (UUID, Foreign Key → sessions.id, Not Null)
course_id (UUID, Foreign Key → courses.id, Not Null)
status (Enum: present, absent, late, Default: present)
device_fingerprint (Text, Nullable)
location (Text, Nullable)
created_at (Timestamp, Default: now())

UNIQUE (student_id, session_id)
```

#### 6. **live_sessions** (جلسات البث المباشر)
```sql
id (UUID, Primary Key)
course_id (UUID, Foreign Key → courses.id, Not Null)
instructor_id (UUID, Foreign Key → profiles.id, Not Null)
title (Text, Not Null)
description (Text, Nullable)
room_name (Text, Unique, Not Null) - اسم غرفة Jitsi
meeting_url (Text, Not Null) - رابط Jitsi
scheduled_start (Timestamp, Not Null)
scheduled_end (Timestamp, Not Null)
actual_start (Timestamp, Nullable)
actual_end (Timestamp, Nullable)
status (Enum: scheduled, live, ended, cancelled, Default: scheduled)
viewers_count (Integer, Default: 0)
created_at (Timestamp)
updated_at (Timestamp)
```

#### 7. **notifications** (الإشعارات)
```sql
id (UUID, Primary Key)
user_id (UUID, Foreign Key → profiles.id, Not Null)
title (Text, Not Null)
title_ar (Text, Not Null)
message (Text, Not Null)
message_ar (Text, Not Null)
type (Enum: info, success, warning, error, Default: info)
link (Text, Nullable)
is_read (Boolean, Default: false)
created_at (Timestamp, Default: now())
```

#### 8. **device_sessions** (جلسات الأجهزة - للأمان)
```sql
id (UUID, Primary Key)
user_id (UUID, Foreign Key → profiles.id, Not Null)
device_fingerprint (Text, Not Null)
ip_address (Text, Nullable)
user_agent (Text, Nullable)
last_active (Timestamp, Default: now())
created_at (Timestamp, Default: now())

UNIQUE (user_id, device_fingerprint)
```

### Row Level Security (RLS):
- يجب تفعيل RLS على جميع الجداول
- Policies حسب الدور:
  - Admin: كل الصلاحيات
  - Instructor: قراءة وتعديل بياناته ومقرراته فقط
  - Student: قراءة مقرراته وحضوره فقط
  - Supervisor: قراءة فقط

---

## 🔧 Backend (Edge Functions)

### التقنية المستخدمة:
- **Supabase Edge Functions** (Deno)
- **Hono Framework** لـ Routing

### الـ Edge Function موجود على:
```
https://pcymgqdjbdklrikdquih.supabase.co/functions/v1/server
```

### جميع الـ Routes تبدأ بـ:
```
/make-server-90ad488b
```

### الـ Endpoints المطلوبة:

#### Authentication:
```
POST /make-server-90ad488b/signup - إنشاء حساب جديد
GET  /make-server-90ad488b/me - الحصول على المستخدم الحالي
```

#### Users:
```
GET /make-server-90ad488b/users - جميع المستخدمين (Admin)
```

#### Courses:
```
GET    /make-server-90ad488b/courses - جميع المقررات
POST   /make-server-90ad488b/courses - إضافة مقرر
PUT    /make-server-90ad488b/courses/:id - تعديل مقرر
DELETE /make-server-90ad488b/courses/:id - حذف مقرر
```

#### Enrollments:
```
GET    /make-server-90ad488b/enrollments - جميع التسجيلات
POST   /make-server-90ad488b/enrollments - تسجيل طالب
DELETE /make-server-90ad488b/enrollments/:id - إلغاء تسجيل
```

#### Sessions:
```
GET    /make-server-90ad488b/sessions - جميع الجلسات
POST   /make-server-90ad488b/sessions - إنشاء جلسة
PUT    /make-server-90ad488b/sessions/:id - تحديث جلسة
DELETE /make-server-90ad488b/sessions/:id - حذف جلسة
```

#### Attendance:
```
GET  /make-server-90ad488b/attendance - سجلات الحضور
POST /make-server-90ad488b/attendance - تسجيل حضور
```

#### Live Sessions:
```
GET  /make-server-90ad488b/live-sessions - جلسات البث
POST /make-server-90ad488b/live-sessions - إنشاء جلسة بث
PUT  /make-server-90ad488b/live-sessions/:id/status - تحديث حالة
```

#### Stats:
```
GET /make-server-90ad488b/stats/public - إحصائيات عامة
GET /make-server-90ad488b/stats/dashboard - إحصائيات Dashboard
```

#### Notifications:
```
GET /make-server-90ad488b/notifications - جميع الإشعارات
PUT /make-server-90ad488b/notifications/:id/read - تعليم كمقروء
```

### الأمان:
- جميع Endpoints (عدا health و signup و stats/public) تحتاج Authorization Header
- التحقق من JWT Token
- التحقق من Role للصلاحيات
- معالجة الأخطاء بشكل صحيح
- رسائل خطأ واضحة (عربي/إنجليزي)

---

## 🚀 Frontend Stack

### التقنيات المطلوبة:

**Framework:**
- **React** (مع TypeScript)
- أو **Next.js** (إذا كان أفضل للمشروع)

**Styling:**
- **Tailwind CSS** (إلزامي)
- استخدم Tailwind v4.0
- لا تستخدم CSS Modules أو Styled Components

**State Management:**
- **React Context API** (للمصادقة والمستخدم)
- **Zustand** أو **React Query** (للبيانات)

**Routing:**
- **React Router** (إذا React)
- **Next.js Router** (إذا Next.js)

**Forms:**
- **React Hook Form** (مع Zod للـ Validation)

**HTTP Client:**
- **Fetch API** أو **Axios**

**Real-time:**
- **Supabase Realtime** للإشعارات

**Charts:**
- **Recharts** (للرسوم البيانية)

**Icons:**
- **Lucide React** (إلزامي - لا تستخدم Font Awesome أو Material Icons)

**Date/Time:**
- **date-fns** أو **Day.js**

**Notifications:**
- **Sonner** أو **React Hot Toast**

**Jitsi:**
- **Jitsi Meet API** أو iframe embed

---

## 📱 الصفحات المطلوبة

### 1. صفحة الهبوط (Landing Page) `/`
- Hero Section مع شعار الجامعة
- نبذة عن النظام
- الإحصائيات العامة (مباشرة من Backend)
- أزرار تسجيل دخول / تسجيل
- Footer مع روابط

### 2. صفحة تسجيل الدخول/التسجيل `/auth`
- Tabs (تسجيل دخول / تسجيل جديد)
- Form مع Validation
- رسائل خطأ واضحة
- زر تبديل اللغة

### 3. لوحة تحكم Admin `/admin`
- Sidebar مع القوائم
- Dashboard بالإحصائيات والرسوم البيانية
- صفحات فرعية:
  - `/admin/users` - إدارة المستخدمين
  - `/admin/courses` - إدارة المقررات
  - `/admin/enrollments` - إدارة التسجيلات
  - `/admin/sessions` - عرض جميع الجلسات
  - `/admin/attendance` - عرض سجلات الحضور
  - `/admin/reports` - التقارير

### 4. لوحة تحكم Instructor `/instructor`
- Sidebar
- Dashboard بإحصائيات مقرراته
- صفحات فرعية:
  - `/instructor/courses` - مقرراتي
  - `/instructor/sessions` - جلساتي
  - `/instructor/sessions/create` - إنشاء جلسة جديدة
  - `/instructor/live-sessions` - جلسات البث المباشر
  - `/instructor/live-sessions/create` - إنشاء جلسة بث
  - `/instructor/attendance` - سجلات الحضور
  - `/instructor/reports` - تقارير مقرراتي

### 5. لوحة تحكم Student `/student`
- Sidebar
- Dashboard بإحصائياته
- صفحات فرعية:
  - `/student/courses` - مقرراتي
  - `/student/attendance` - سجل حضوري
  - `/student/attendance/submit` - تسجيل حضور (إدخال الكود)
  - `/student/live-sessions` - الجلسات المباشرة
  - `/student/notifications` - الإشعارات

### 6. لوحة تحكم Supervisor `/supervisor`
- Sidebar
- Dashboard بالإحصائيات العامة
- صفحات فرعية:
  - `/supervisor/courses` - جميع المقررات (قراءة فقط)
  - `/supervisor/sessions` - جميع الجلسات
  - `/supervisor/attendance` - سجلات الحضور
  - `/supervisor/reports` - التقارير الشاملة

### 7. صفحات مشتركة
- `/profile` - الملف الشخصي
- `/settings` - الإعدادات
- `/notifications` - جميع الإشعارات
- `/404` - صفحة غير موجودة
- `/unauthorized` - غير مصرح

---

## 🔒 الأمان (Security Requirements)

### 1. منع تسجيل الدخول المتزامن:
- تسجيل Device Fingerprint لكل جلسة
- عند تسجيل دخول من جهاز جديد، إنهاء الجلسات السابقة
- إشعار المستخدم بتسجيل دخول من جهاز جديد

### 2. Device Fingerprint:
- استخدم مكتبة مثل `@fingerprintjs/fingerprintjs`
- تسجيل: Browser, OS, Screen Resolution, Timezone, etc.
- تخزين في جدول `device_sessions`

### 3. JWT Token:
- Expiration: 7 أيام
- Refresh Token mechanism
- تخزين في httpOnly Cookie (إن أمكن) أو Local Storage

### 4. Rate Limiting:
- تحديد عدد محاولات تسجيل الدخول (5 محاولات / 15 دقيقة)
- تحديد عدد محاولات تسجيل الحضور

### 5. SQL Injection Protection:
- استخدام Parameterized Queries (Supabase يدعم ذلك تلقائياً)

### 6. XSS Protection:
- تنظيف جميع المدخلات من المستخدم
- استخدام DOMPurify (إن لزم)

### 7. CORS:
- السماح فقط للـ Origins المصرح بها

---

## 📊 متطلبات الأداء (Performance)

1. **سرعة التحميل:**
   - First Contentful Paint < 1.5 ثانية
   - Time to Interactive < 3 ثوان

2. **Optimization:**
   - Lazy Loading للصور والمكونات الثقيلة
   - Code Splitting
   - Minification & Compression
   - Caching

3. **Database:**
   - استخدام Indexes على الأعمدة المستخدمة في البحث
   - تحسين Queries

4. **Real-time:**
   - استخدام Supabase Realtime بكفاءة
   - Unsubscribe عند مغادرة الصفحة

---

## 🧪 الاختبار (Testing)

### ليس إلزامياً، لكن مفضل:
- Unit Tests للـ Functions المهمة
- Integration Tests للـ API Endpoints
- E2E Tests لـ User Flows الأساسية

---

## 📦 التسليمات المطلوبة (Deliverables)

### 1. الكود:
- مستودع Git (GitHub أو GitLab)
- Branch: `main` أو `master`
- Commits واضحة ومنظمة
- `.gitignore` صحيح (لا `.env` في Git)

### 2. التوثيق:
- `README.md` شامل:
  - نظرة عامة عن المشروع
  - خطوات التثبيت والتشغيل
  - متغيرات البيئة `.env.example`
  - البنية الأساسية للمشروع
  - التقنيات المستخدمة
  - Endpoints الـ API
  - كيفية تطبيق Edge Functions

- `SETUP.md`:
  - خطوات إعداد Supabase
  - إنشاء الجداول (SQL Scripts)
  - إعداد RLS Policies
  - إعداد Edge Functions

- `DATABASE_SCHEMA.md`:
  - مخطط قاعدة البيانات الكامل
  - العلاقات بين الجداول

### 3. الملفات:
- جميع SQL Scripts لإنشاء الجداول
- ملف `.env.example` مع شرح كل متغير
- ملف `package.json` مع جميع Dependencies

### 4. الـ Deployment:
- تطبيق Edge Functions على Supabase
- التطبيق يعمل بشكل كامل على Supabase

---

## ⚠️ الشروط والمتطلبات الإلزامية

### ✅ يجب:
1. استخدام **Supabase** فقط (PostgreSQL + Edge Functions)
2. استخدام **React** أو **Next.js**
3. استخدام **Tailwind CSS** فقط
4. استخدام **Lucide React** للأيقونات
5. استخدام **Jitsi Meet** للبث المباشر
6. دعم **اللغتين** (عربي/إنجليزي) بشكل كامل
7. دعم **RTL/LTR** تلقائي
8. استخدام ألوان **جامعة الملك خالد** (`#006747`)
9. **لا بيانات وهمية** - كل شيء حقيقي من قاعدة البيانات
10. **Edge Functions** على الرابط الموجود مع Prefix `/make-server-90ad488b`
11. كود نظيف ومنظم مع **Comments** واضحة
12. **Responsive** على جميع الأجهزة
13. جميع الميزات **تعمل 100%** بدون أخطاء

### ❌ ممنوع:
1. استخدام Firebase أو MongoDB
2. بيانات Mock أو Dummy
3. أكواد ثابتة Hardcoded (يجب أن تكون ديناميكية)
4. CSS Modules أو Styled Components
5. Font Awesome أو Material Icons
6. Zoom أو Google Meet للبث المباشر
7. ترك أي أخطاء في Console
8. واجهة غير احترافية

---

## 💰 الميزانية والوقت

**الميزانية:** [حدد الميزانية هنا]

**المدة المتوقعة:** [حدد المدة هنا]

**الدفع:**
- [حدد طريقة الدفع: دفعة واحدة / على مراحل]
- [إذا على مراحل: حدد النسب - مثلاً 30% عند البدء، 40% عند إكمال Backend، 30% عند التسليم النهائي]

---

## 📞 التواصل والدعم

**بعد التسليم:**
- فترة دعم: [حدد الفترة - مثلاً: شهر مجاناً]
- إصلاح الأخطاء المكتشفة خلال الفترة
- شرح الكود والنظام (جلسة أو جلستين)

**التواصل أثناء التطوير:**
- تحديثات دورية (مثلاً: كل 3 أيام)
- إمكانية مراجعة التقدم
- اجتماعات عبر Zoom/Google Meet (إذا لزم)

---

## 📋 ملاحظات إضافية

1. **الأولوية:**
   - الميزات الأساسية أولاً (Auth, Courses, Sessions, Attendance)
   - ثم الميزات المتقدمة (Live Sessions, Notifications, Stats)

2. **الجودة:**
   - كود نظيف وقابل للصيانة
   - تعليقات Comments على الأجزاء المهمة
   - Naming Conventions واضحة

3. **الأمان:**
   - حماية جميع Endpoints
   - Validation على Frontend و Backend
   - معالجة الأخطاء بشكل صحيح

4. **التوثيق:**
   - README شامل ومفصل
   - شرح كل خطوة من خطوات الإعداد
   - SQL Scripts جاهزة للتنفيذ

5. **الاختبار:**
   - اختبار جميع الميزات قبل التسليم
   - اختبار على Chrome, Safari, Firefox
   - اختبار على Desktop, Tablet, Mobile

---

## ✅ معايير القبول (Acceptance Criteria)

سأقبل المشروع إذا:

1. ✅ جميع الميزات المذكورة تعمل 100%
2. ✅ لا أخطاء في Console
3. ✅ التصميم احترافي ومطابق للمواصفات
4. ✅ الألوان مطابقة لألوان جامعة الملك خالد
5. ✅ دعم اللغتين كامل و RTL/LTR يعمل
6. ✅ Responsive على جميع الأجهزة
7. ✅ Backend (Edge Functions) يعمل بشكل صحيح
8. ✅ قاعدة البيانات مُعدّة بشكل صحيح
9. ✅ التوثيق شامل ومفصل
10. ✅ الكود نظيف ومنظم
11. ✅ النظام آمن ومحمي
12. ✅ الأداء ممتاز (سريع)
13. ✅ جلسات Jitsi تعمل
14. ✅ الإشعارات الفورية تعمل
15. ✅ التقارير والإحصائيات صحيحة

---

## 🎯 الخلاصة

**المطلوب:** نظام حضور ذكي متكامل لجامعة الملك خالد، احترافي، يعمل بشكل حقيقي، مع 4 أدوار مختلفة، جلسات بث مباشر، إشعارات فورية، أمان متقدم، تصميم بألوان الجامعة، دعم لغتين، Responsive، بدون بيانات وهمية.

**التقنيات الإلزامية:**
- Frontend: React/Next.js + Tailwind CSS
- Backend: Supabase Edge Functions (Deno + Hono)
- Database: Supabase (PostgreSQL)
- Live Streaming: Jitsi Meet
- Icons: Lucide React
- Charts: Recharts

**الألوان:** `#006747` (الأخضر الداكن - لون جامعة الملك خالد)

**Edge Function URL:** `https://pcymgqdjbdklrikdquih.supabase.co/functions/v1/make-server-90ad488b`

**لا بيانات وهمية - كل شيء حقيقي من قاعدة البيانات!**

---

**إذا كنت مهتماً، تواصل معي لمناقشة التفاصيل والميزانية والمدة الزمنية. 🚀**

---

**تاريخ إنشاء الوثيقة:** 14 ديسمبر 2024
**الإصدار:** 1.0
