# 📚 دليل النشر الشامل خطوة بخطوة

## نظام الحضور الذكي - جامعة الملك خالد

هذا الدليل سيعلمك **بالتفصيل** كيفية:
1. ✅ نشر Edge Functions على Supabase
2. ✅ نشر المشروع على Vercel
3. ✅ ربط كل شيء معاً

---

# 📋 المتطلبات الأساسية

قبل أن نبدأ، تأكد من تثبيت:

1. **Node.js** (الإصدار 18 أو أحدث)
   ```bash
   node --version
   ```

2. **Git** (للتعامل مع GitHub)
   ```bash
   git --version
   ```

3. **Supabase CLI** (لنشر Edge Functions)
   ```bash
   npm install -g supabase
   ```

4. حساب على:
   - ✅ [Supabase](https://supabase.com) (مجاني)
   - ✅ [Vercel](https://vercel.com) (مجاني)
   - ✅ [GitHub](https://github.com) (مجاني)

---

# 🎯 الجزء الأول: نشر Edge Functions على Supabase

## الخطوة 1: تسجيل الدخول إلى Supabase CLI

افتح Terminal (أو Command Prompt) في مجلد المشروع:

```bash
supabase login
```

ستفتح نافذة متصفح تطلب منك تسجيل الدخول. سجل الدخول بحساب Supabase الخاص بك.

### ✅ التحقق من نجاح تسجيل الدخول:
بعد تسجيل الدخول، ستظهر رسالة:
```
✔ Logged in successfully!
```

---

## الخطوة 2: الحصول على Project ID من Supabase

### الطريقة الأولى: من موقع Supabase
1. افتح [supabase.com](https://supabase.com)
2. اذهب إلى مشروعك (King Khalid Attendance System)
3. اضغط على **Settings** (الإعدادات) في القائمة الجانبية
4. اضغط على **General** أو **API**
5. ستجد **Reference ID** أو **Project ID** مثل: `abcdefghijklmnop`

### الطريقة الثانية: من Terminal
```bash
supabase projects list
```

ستظهر قائمة بمشاريعك مع الـ ID لكل مشروع.

---

## الخطوة 3: ربط المشروع المحلي مع Supabase

استخدم الـ Project ID الذي حصلت عليه:

```bash
supabase link --project-ref YOUR_PROJECT_ID
```

**مثال:**
```bash
supabase link --project-ref abcdefghijklmnop
```

### ✅ التحقق من نجاح الربط:
ستظهر رسالة:
```
✔ Linked to project abcdefghijklmnop
```

---

## الخطوة 4: نشر Edge Functions

الآن جاء وقت النشر! 🚀

```bash
supabase functions deploy server
```

### 📝 ماذا يحدث خلال النشر؟
1. يقوم CLI برفع ملف `/supabase/functions/server/index.tsx` إلى Supabase
2. يقوم Supabase بـ compile الكود
3. يجعل الـ function متاحة على الإنترنت

### ✅ رسائل النجاح التي ستظهر:
```
Bundling server...
Deploying server (project ref: abcdefghijklmnop)
✔ Deployed Function server
```

### 🌐 رابط الـ Function الخاص بك:
بعد النشر، ستجد رابط الـ function:
```
https://YOUR_PROJECT_ID.supabase.co/functions/v1/server
```

---

## الخطوة 5: التحقق من نجاح نشر Edge Functions

### طريقة 1: من Terminal
```bash
supabase functions list
```

يجب أن تظهر:
```
NAME      VERSION   STATUS    CREATED_AT
server    1         active    2025-XX-XX
```

### طريقة 2: من موقع Supabase
1. افتح [supabase.com](https://supabase.com)
2. اذهب إلى مشروعك
3. في القائمة الجانبية، اضغط على **Edge Functions**
4. يجب أن ترى function اسمها **server**

### طريقة 3: اختبار الـ API
افتح متصفح أو استخدم curl:

```bash
curl https://YOUR_PROJECT_ID.supabase.co/functions/v1/make-server-90ad488b/health
```

يجب أن ترى:
```json
{"status":"ok"}
```

---

## الخطوة 6: مشاهدة Logs

لمشاهدة logs الخاصة بالـ Edge Function:

```bash
supabase functions logs server --follow
```

هذا سيعرض لك جميع الطلبات والأخطاء في الوقت الفعلي.

---

# 🎯 الجزء الثاني: إعداد قاعدة البيانات على Supabase

## الخطوة 1: فتح SQL Editor

1. افتح [supabase.com](https://supabase.com)
2. اذهب إلى مشروعك
3. في القائمة الجانبية، اضغط على **SQL Editor**

---

## الخطوة 2: تنفيذ SQL Schema

1. في Figma Make، افتح ملف `/database_schema.sql`
2. **انسخ المحتوى بالكامل**
3. ارجع إلى **SQL Editor** في Supabase
4. الصق الكود
5. اضغط على **Run** (تشغيل)

### ✅ ماذا يحدث؟
سيتم إنشاء جميع الجداول:
- ✅ `profiles` - جدول المستخدمين
- ✅ `courses` - جدول المقررات
- ✅ `enrollments` - جدول التسجيلات
- ✅ `sessions` - جدول الجلسات
- ✅ `attendance` - جدول الحضور
- ✅ `schedules` - جدول الجداول الدراسية

---

## الخطوة 3: التحقق من إنشاء الجداول

في **SQL Editor**، شغّل هذا الاستعلام:

```sql
SELECT tablename 
FROM pg_tables 
WHERE schemaname = 'public'
ORDER BY tablename;
```

يجب أن ترى جميع الجداول المذكورة أعلاه.

---

## الخطوة 4: إضافة بيانات تجريبية (اختياري)

لاختبار النظام، يمكنك إضافة بعض البيانات:

```sql
-- إضافة Admin
INSERT INTO profiles (id, email, full_name, role, university_id)
VALUES (
  gen_random_uuid(),
  'admin@kku.edu.sa',
  'مدير النظام',
  'admin',
  NULL
);

-- إضافة مدرس
INSERT INTO profiles (id, email, full_name, role, university_id)
VALUES (
  gen_random_uuid(),
  'instructor@kku.edu.sa',
  'د. محمد أحمد',
  'instructor',
  NULL
);

-- إضافة طالب
INSERT INTO profiles (id, email, full_name, role, university_id)
VALUES (
  gen_random_uuid(),
  'student@kku.edu.sa',
  'أحمد محمد علي',
  'student',
  '441234567'
);

-- إضافة مقرر
INSERT INTO courses (id, course_name, course_code, instructor_id)
SELECT 
  gen_random_uuid(),
  'برمجة الويب المتقدمة',
  'CS301',
  id
FROM profiles 
WHERE role = 'instructor' 
LIMIT 1;
```

---

# 🎯 الجزء الثالث: نشر المشروع على Vercel

## الخطوة 1: رفع المشروع على GitHub (إذا لم يكن مرفوعاً)

### إنشاء Repository جديد على GitHub:

1. افتح [github.com](https://github.com)
2. اضغط على **New Repository**
3. اسم Repository: `kku-attendance-system`
4. اختر **Private** (خاص)
5. **لا تضف** README أو .gitignore
6. اضغط **Create repository**

### ربط المشروع المحلي بـ GitHub:

في Terminal، في مجلد المشروع:

```bash
# تهيئة git (إذا لم يكن مهيئاً)
git init

# إضافة جميع الملفات
git add .

# عمل commit
git commit -m "Initial commit - KKU Attendance System"

# ربط بـ GitHub (استبدل USERNAME بـ username الخاص بك)
git remote add origin https://github.com/USERNAME/kku-attendance-system.git

# رفع الكود
git branch -M main
git push -u origin main
```

---

## الخطوة 2: ربط Vercel بـ GitHub

1. افتح [vercel.com](https://vercel.com)
2. سجل الدخول (Sign in with GitHub)
3. اضغط على **Add New...** → **Project**
4. اختر repository: `kku-attendance-system`
5. اضغط **Import**

---

## الخطوة 3: إعداد Environment Variables على Vercel

قبل النشر، يجب إضافة المتغيرات البيئية!

### في صفحة إعدادات المشروع على Vercel:

1. ابحث عن قسم **Environment Variables**
2. أضف المتغيرات التالية:

#### المتغير 1: VITE_SUPABASE_URL
```
Name: VITE_SUPABASE_URL
Value: https://YOUR_PROJECT_ID.supabase.co
```

**كيف تحصل على القيمة؟**
- افتح Supabase → Settings → API → Project URL

#### المتغير 2: VITE_SUPABASE_ANON_KEY
```
Name: VITE_SUPABASE_ANON_KEY
Value: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

**كيف تحصل على القيمة؟**
- افتح Supabase → Settings → API → Project API keys → `anon` `public`

#### المتغير 3: VITE_SUPABASE_SERVICE_ROLE_KEY (مهم!)
```
Name: VITE_SUPABASE_SERVICE_ROLE_KEY
Value: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

**كيف تحصل على القيمة؟**
- افتح Supabase → Settings → API → Project API keys → `service_role` `secret`

⚠️ **تحذير أمني مهم:**
- **لا ترفع** `service_role_key` إلى Frontend!
- هذا المفتاح **فقط** للـ Backend (Edge Functions)
- في Vercel، المتغيرات التي تبدأ بـ `VITE_` تُرسل للـ Frontend
- لكن نحن **لا نستخدمها** في Frontend - فقط Edge Functions تستخدمها

---

## الخطوة 4: نشر المشروع على Vercel

بعد إضافة Environment Variables:

1. اضغط **Deploy**
2. انتظر 2-3 دقائق
3. عند انتهاء النشر، اضغط **Visit** لفتح الموقع

---

## الخطوة 5: التحقق من الاتصال

### افتح موقعك على Vercel

ستظهر الصفحة الرئيسية. افتح **Console** (اضغط F12):

```
🔍 Fetching landing stats from API...
📡 Response status: 200
✅ Landing page stats from database: 
   - Students: 1
   - Instructors: 1
   - Courses: 1
```

إذا ظهرت هذه الرسائل، معناه **النظام يعمل 100%!** 🎉

---

# 🎯 الجزء الرابع: إعداد ملف info.tsx (مهم جداً!)

## المشكلة:
المشروع يحتوي على ملف `/utils/supabase/info.example.tsx` لكن يحتاج `info.tsx` حقيقي.

## الحل:

### الخطوة 1: إنشاء ملف info.tsx

في Figma Make، افتح `/utils/supabase/info.tsx` وتأكد من أنه يحتوي على:

```typescript
export const projectId = 'YOUR_PROJECT_ID'; // مثل: abcdefghijklmnop
export const publicAnonKey = 'YOUR_ANON_KEY'; // من Supabase → Settings → API
```

### الخطوة 2: استبدال القيم

1. افتح Supabase → Settings → API
2. انسخ **Project URL** واستخرج منه الـ `PROJECT_ID`
   - مثال: `https://abcdefghijklmnop.supabase.co` → `projectId = 'abcdefghijklmnop'`
3. انسخ **anon public** key
4. الصق القيم في `/utils/supabase/info.tsx`

### الخطوة 3: رفع التحديث

```bash
git add .
git commit -m "Add Supabase info"
git push
```

Vercel سيقوم بـ re-deploy تلقائياً! ✨

---

# 🎯 الجزء الخامس: اختبار النظام الكامل

## الاختبار 1: تسجيل مستخدم جديد

1. افتح موقعك على Vercel
2. اضغط على **تسجيل الدخول / Sign Up**
3. اختر **Sign Up**
4. املأ البيانات:
   - Email: `test@kku.edu.sa`
   - Password: `Test123456`
   - Full Name: `محمد أحمد`
   - Role: `Student`
   - University ID: `441234567`
5. اضغط **Sign Up**

### ✅ النتيجة المتوقعة:
- تسجيل ناجح ✅
- تسجيل دخول تلقائي
- الانتقال إلى لوحة تحكم الطالب

---

## الاختبار 2: التحقق من قاعدة البيانات

في Supabase SQL Editor:

```sql
-- عرض جميع المستخدمين
SELECT * FROM profiles;

-- عرض جميع المقررات
SELECT * FROM courses;

-- عرض الإحصائيات
SELECT 
  (SELECT COUNT(*) FROM profiles WHERE role = 'student') as students,
  (SELECT COUNT(*) FROM profiles WHERE role = 'instructor') as instructors,
  (SELECT COUNT(*) FROM courses) as courses;
```

---

## الاختبار 3: التحقق من الإحصائيات في الصفحة الرئيسية

1. ارجع إلى الصفحة الرئيسية (اضغط **تسجيل خروج**)
2. افتح Console (F12)
3. أعد تحميل الصفحة
4. ابحث عن:
   ```
   ✅ Public stats retrieved from REAL DATABASE
   ```

---

# 🎯 الجزء السادس: إضافة Custom Domain (اختياري)

## على Vercel:

1. اذهب إلى **Settings** → **Domains**
2. أضف domain الخاص بك (مثل: `attendance.kku.edu.sa`)
3. اتبع التعليمات لإضافة DNS records

---

# 📋 ملخص سريع للأوامر

```bash
# 1. تسجيل الدخول إلى Supabase
supabase login

# 2. ربط المشروع
supabase link --project-ref YOUR_PROJECT_ID

# 3. نشر Edge Functions
supabase functions deploy server

# 4. مشاهدة Logs
supabase functions logs server --follow

# 5. رفع على GitHub
git add .
git commit -m "Deploy to Vercel"
git push

# 6. Vercel ستقوم بـ deploy تلقائياً!
```

---

# ❌ حل المشاكل الشائعة

## مشكلة 1: `supabase: command not found`

**الحل:**
```bash
npm install -g supabase
```

---

## مشكلة 2: `Project not linked`

**الحل:**
```bash
supabase link --project-ref YOUR_PROJECT_ID
```

---

## مشكلة 3: الأرقام تظهر "0" في الصفحة الرئيسية

**الأسباب المحتملة:**
1. Edge Functions لم تُنشر بعد
2. قاعدة البيانات فارغة
3. Environment Variables خاطئة

**الحل:**
```bash
# 1. تحقق من نشر Edge Functions
supabase functions list

# 2. تحقق من قاعدة البيانات
# في Supabase SQL Editor:
SELECT COUNT(*) FROM profiles WHERE role = 'student';

# 3. تحقق من Logs
supabase functions logs server
```

---

## مشكلة 4: خطأ CORS في Console

**الحل:**
تأكد من أن Edge Functions تتضمن CORS headers (موجودة بالفعل في الكود).

---

## مشكلة 5: `Error: Missing environment variables`

**الحل في Vercel:**
1. اذهب إلى **Settings** → **Environment Variables**
2. أضف:
   - `VITE_SUPABASE_URL`
   - `VITE_SUPABASE_ANON_KEY`
3. **Redeploy** المشروع

---

# 🎉 تهانينا!

الآن لديك نظام حضور ذكي كامل يعمل على:
- ✅ **Frontend**: مستضاف على Vercel
- ✅ **Backend**: Edge Functions على Supabase
- ✅ **Database**: PostgreSQL على Supabase
- ✅ **مربوط ببيانات حقيقية 100%**

---

# 📞 الدعم

إذا واجهت أي مشكلة:
1. راجع قسم **حل المشاكل الشائعة** أعلاه
2. تحقق من Logs: `supabase functions logs server`
3. افتح Console في المتصفح (F12) وابحث عن الأخطاء

---

**تم بنجاح! 🎊**
